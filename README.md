#### Description
Extract `rgssad` / `rgss2a` / `rgss3a` files.

#### Usage
```
Commands:
    help
    version
    list        <archive>
    unpack      <archive> <folder> [<filter>]
    pack        <folder> <archive> [<version>]
```
